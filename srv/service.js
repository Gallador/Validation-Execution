const cds = require('@sap/cds');
const db = cds.db;

module.exports = cds.service.impl(async function () {

    let { ValidationExecution, CheckSet, ScopeOfCheck, ValidationExecParamValues, ValidationExecMessages } = this.entities;

})